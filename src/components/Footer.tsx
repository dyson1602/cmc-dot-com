import '../styles/footer.css';
import { ScrollToTopButton } from './ScrollToTopButton';

export const Footer = () => {
  return (
    <section id="footer">
      <div>
        <ScrollToTopButton />
        FOOTER</div>
    </section>
  );
};
